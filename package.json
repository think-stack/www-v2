{
  "private": true,
  "name": "sanity-gatsby-blog",
  "version": "1.0.6",
  "scripts": {
    "build": "npm run build --parallel",
    "dev": "npm run dev --parallel",
    "format": "npm run format",
    "build-studio": "npm bootstrap && cd studio && npm run build",
    "build-web": "npm bootstrap && (cd studio && SANITY_AUTH_TOKEN=$SANITY_DEPLOY_STUDIO_TOKEN npm run graphql-deploy) && (cd web && npm run build)",
    "graphql-deploy": "npm run graphql-deploy",
    "lint": "npm run lint",
    "postinstall": "npm bootstrap",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@sanity/cli": "^0.147.8"
  }
}
